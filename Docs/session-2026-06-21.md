# Session Context — 2026-06-21 (Phase 2 Polish: real artist art — ingredients, bacon, cook, plates)

Phase 2 (Polish & Final). Branch: `polish`. First big art-integration pass: brought the artist's
delivered sprites into the project, added a new **Bacon** ingredient, reworked the **cook** to use
real front/flipped art, and added decorative **plates** under each column. Raw drop lives in
`Assets/RawArt/` (now gitignored; local source-of-truth — only finals are committed under `_Project`).

## What shipped

### Ingredient sprites + Bacon (the new 4th ingredient)
- Swapped all 9 ingredient finals in over the existing files (Path A — kept guid/fileID so the
  `Resources/Skins` refs survive): meat (`Burger.png`), cheese, tomato, onion, pickle, lettuce, egg,
  bun_top (`Bread_Top`), bun_bottom (`Bread_Bottom`). Each meta got a per-file `spritePixelsToUnits`
  to normalise on-screen size (target width ~1.2; buns +15%; meat/tomato/onion −6%, cheese +10%,
  lettuce −4% per playtest).
- **Bacon** added as the 4th ingredient: `IngredientType.Bacon = 7` (append; int not load-bearing) →
  inserted at index 3 of `GameplayConfig.REGULAR_INGREDIENTS` (appears from L1); `SkinSlot.BaconSkin`
  (append, 11) + `SkinMap` entry + `bacon_default.asset`. Difficulty +1: `STARTING_INGREDIENT_COUNT`
  3→4, `MAX_INGREDIENT_COUNT` 7→8, every `INGREDIENT_COUNT_BY_LEVEL` cell +1.

### Cook (Dogtor) — front + flipped, real 3D flip
- `chef.png` ← `Dogtor_Front`, new `chef_flipped.png` ← `Dogtor_Flipped_` (PPU 663). `chef_default`
  now dual-sprite (`_sprite` front, `_secondarySprite` flipped); `Theme.ChefFlipped` added.
- `ChefController.SwapPlates` keeps the 3D Y-rotation but swaps the SpriteRenderer Front↔Flipped at the
  edge-on midpoint + toggles `flipX` to cancel the 180° mirror (badge reads correct). Single renderer.
- Removed the old white position-bubble visuals (kept `BubbleRadius` value for tap detection); deleted
  now-dead `SORT_CHEF_BUBBLE`, `BUBBLE_ACTIVE/INACTIVE`.

### Plates (decorative, static)
- `PlateSkin` slot + `Theme.Plate`; `plate.png` (PPU 756, mipmaps + uncompressed). `plate_default.asset`.
- `PlateManager` (plain MonoBehaviour, ensured by `GameManager`): spawns 4 static plates, one under each
  column at `GRID_ORIGIN_Y − PLATE_Y_OFFSET (0.2)`. Sort `SORT_CHEF (-3) < SORT_PLATE (-2) < ingredients`
  so the bottom ingredient sits on the plate and the chef's hands tuck under.
- NOTE: an earlier flip-with-the-cook version was built then removed — plates are identical per column,
  so swapping them is a visual no-op. Kept static per dev call.

### Other
- Burger-complete compress loosened ~45% total (`COMPRESS_TRAVEL_SPACING_MULT` 0.2→0.3,
  `COMPRESS_SMACK_SPACING_MULT` 0.15→0.225) — "less clumped".
- Mipmaps enabled on all gameplay sprites (anti-crispy when minified).

## Gotchas resolved (now in CLAUDE.md → Asset Conventions)
- **Sprite internalID collision**: chef.png & chef_alt shared the same sprite fileID (chef_alt was
  copied from chef's meta with only the guid changed) → cross-wired refs (chef invisible / wrong art).
  Fixed by giving chef.png a unique internalID and repointing `chef_default` + `Game.unity`.
- **Large-source import**: chef (2387px) > `maxTextureSize` 2048 → texture downscaled below the pinned
  sprite rect → "rect out of bounds" error, which wiped the meta's sprite (`sprites: []`) → invisible.
  Fixed with `maxTextureSize: 4096`. Backgrounds (2604px) will need the same.
- **Crispy plate**: fine thin lines minified without mipmaps aliased. Fixed with mipmaps (+ uncompressed).

## Verified by dev (in Unity)
Ingredients + bacon look good (sizes hand-tuned); cook front/flip works after the reference + import
fixes; plates render under columns; mipmaps applied. Compress spacing approved.

## Pending / deferred
- **Chef size/position** still needs a tuning pass against the plates — next "resize" session.
- ★ glyph in `ChallengeLevel` renders as □ (LiberationSans lacks U+2605) — cosmetic, fix later.
- Backgrounds + UI panels (authored art in `RawArt`) — future batches; backgrounds need 4096 + mipmaps.
- Carried: lower `SETTINGS_LEVEL_CAP` to `MAX_LEVEL` before release.

## Git state
Committed + pushed on `polish`. Left out of the commit: `.claude/settings.local.json` (local perms) and
a stray `ChatGPT Image ….png` in `Sprites/` (unrelated, pre-existing untracked).
