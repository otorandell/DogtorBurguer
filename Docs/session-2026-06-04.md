# Session Context — 2026-06-02 → 2026-06-04

Continuation of the human-led, file-by-file code review begun 2026-05-26.
Supersedes `session-2026-05-28.md` (whose resume point — "open
`Ingredients/Ingredient.cs`" — is now stale; that file and three more
folders are done). Findings logged in `Docs/Review/` (`notes.md` +
`README.md` index). Workflow unchanged (see below).

## Scope covered

8 of ~11 review areas now done:

| Folder | Status | Findings |
|---|---|---|
| `Audio/` | Done | F-1 – F-6 |
| `Burger/` + `Scoring/` | Done (empty placeholders) | F-7 |
| `Chef/` | Done | F-8 – F-10 |
| `Core/` | Done | F-11 – F-25 |
| `Grid/` | Done | F-26 – F-32 |
| `Ingredients/` | **Done this session** | F-33 – F-43 |
| `Input/` | **Done this session** | F-44, F-45 |
| `Monetization/` | **Done this session** | F-46 – F-51 |
| `UI/` | **Next** | — |
| Project root + assets | Pending | — |

**Resume point:** start `UI/` with `UIFactory.cs` (per the review-order
note in README), then `GameLayout`, `Background`, popups (`FloatingText`,
`ScorePopup`, `BurgerPopup`), panels (`MainMenuUI`, `GameHUD`,
`GameOverPanel`, `SettingsPanel`, `ShopPanel`, `BurgerChallenge`).
Then project root + assets last.

## Findings added this session — F-35 through F-51 (17 new; 51 total)

**Ingredients/**
- **F-35** — `SpawnerState` enum nested in `IngredientSpawner` → own file. *(cosmetic)*
- **F-36** — Dead `Idle` state redundant with `_active` (`case Idle` unreachable); **decided Option A: delete it**; `_active` stays as F-21-style modifier.
- **F-37** — Spawner consumes raw level only for `GetWaveSize` triple-chance → move computation to DifficultyManager, push value, delete `SetCurrentLevel`/`_currentLevel` (upgrades the :285 "acceptable" note).
- **F-38** — Extract `WaveComposer` from the 378-line `IngredientSpawner` (SRP split of "what to spawn"; **not** an animation split).
- **F-39** — `IngredientType` int values load-bearing in 4 sites → `GameplayConfig.REGULAR_INGREDIENTS` list; enum order then free (buns→0,1 optional). Kills the extensions class (with F-27). Verified no serialization risk.
- **F-40** — Promote `(IngredientType, int columnIndex)` wave-slot tuple to named `WaveSlot` struct (shares F-32).
- **F-41** — `_data`/`_previews` parallel lists can desync (latent `TryTap` index bug) → collapse to one `(preview, slot)` list; folds in DOTween/teardown nits.
- **F-42** — `CreatePreview` takes `Column` but uses only `ColumnIndex` → drop to `int`, removes the manager's sole `GridManager` coupling.
- **F-43** — `sortingOrder = 90` magic number → named constant. **Promoted to a codebase-wide sorting-order sweep** when the 2nd sighting hit (F-50).

**Input/**
- **F-44** — Defensive Awake dependency resolution, **3rd sighting → formalized codebase-wide**. Kill the `FindAnyObjectByType` scene-scan variant first; standardize on explicit injection. (README tracker closed.)
- **F-45** — `TouchInputHandler` gesture-vs-mode tangle: dedup swipe-move + preview/falling tap, rename `ProcessTap`/`ProcessTapMode`; folds in magic numbers, `ControlMode.Drag` silent default, `_isDragging` rename.

**Monetization/**
- **F-46** — Singleton guard boilerplate duplicated across ~7 managers + missing `Instance` null-on-destroy → `Singleton<T>` base (guard/teardown **only**, NO lazy auto-create — must preserve documented init order).
- **F-47** — 🔴 **BLOCKER**: `GemPack` is invisible — `SpriteRenderer` created but `.sprite` never assigned (built from bare GameObject, no prefab). Confirmed with dev: **a real bug, not an intentional placeholder.**
- **F-48** — `GemPack.OnMouseDown` bypasses the New Input System — device-blocker if Active Input Handling is New-only; route taps through `TouchInputHandler`.
- **F-49** — `GemPack` animation interleaved with construction/logic in `Initialize` + `Collect` (F-18/26/30 lineage).
- **F-50** — Gem-pack magic numbers → **new `MonetizationConfig`** (concrete justification for F-16; advances F-14). Promotes F-43 sorting-order sweep; folds dead `_collider`, redundant tween kill, UI-color-on-world.
- **F-51** — `GemPackSpawner` subscribe-once init-order assumption → silent permanent no-op if `Start` runs before `GameManager`. Read state directly in `Update` instead. (Cousin of F-44.)

**AdManager** mock internals were deliberately **not** reviewed — they're
throwaway placeholders replaced when the real SDK lands (see below).

## Cross-folder threads now tracked (for the eventual synthesis/execution pass)

- **Anonymous structure → named type**: F-32 (`MatchResult`) ↔ F-40 (`WaveSlot`).
- **Parallel-state / stay-in-sync**: F-21 (`Paused`), F-33 (`Ingredient`) ↔ F-41 (`_data`/`_previews`).
- **Inline value belongs in config**: F-24, F-25 ↔ F-43 (sorting orders), F-50 (gem-pack tuning → MonetizationConfig).
- **Defensive dependency resolution**: F-44 (formalized) ↔ F-51 (init-order no-op).
- **Config-file architecture**: F-14 + F-16 now have a concrete trigger via F-50 (`MonetizationConfig`).
- **Singleton hygiene**: F-46 (boilerplate base) relates to F-1 (`EnsureComponent` lift).

## Decisions made this session

- **F-36 Option A**: delete the dead `Idle` state outright (default field to `Delaying`), rather than keep it as a commented construction sentinel.
- **F-39**: decouple `IngredientType` int values via an explicit `REGULAR_INGREDIENTS` list rather than reordering the enum (buns→0,1 becomes optional, not required). Confirmed `SaveDataManager` does not persist the ints, so no migration risk.
- **AdManager realism is out of review scope**: tracked as a pre-launch item, not a finding. The class needs a real ad SDK; interface-hardening could happen now but the SDK wiring is launch-gated.

## Procedural artifacts modified

- `Docs/Review/notes.md` — new sections for `IngredientSpawner`, `IngredientType`, `WavePreviewManager`, `TouchInputHandler`, `AdManager`, `GemPack`+`GemPackSpawner`.
- `Docs/Review/README.md` — index rows F-35–F-51; next tag now **F-52**; defensive-Awake tracker closed (→ F-44).
- `Docs/pre-launch-checklist.md` — added a full **Monetization (Ads)** section (SDK replacement entry + GDPR/ATT consent entry), replacing the old one-line placeholder. Captures the `Time.timeScale` save/restore concern as a mitigation point (cross-ref F-21).
- `Docs/session-2026-06-04.md` — this doc.

## Workflow (unchanged — relevant for next session)

Human-led collaboration:
1. User picks the file, reads it, surfaces observations/questions/doubts.
2. I respond in chat: confirm findings vs not, answer, discuss tradeoffs.
3. We converge.
4. Only then write to `notes.md` + README index together.

`notes.md` is findings-only. Each finding has ONE decisive Next Step.
Procedural changes (memory, CLAUDE.md, doc structure) get discussed
before writing. See memory `feedback_review_workflow.md`.

Note: the F-44/F-46 codebase-wide findings and F-43's promotion show the
"tracking pattern → formalize on Nth sighting" mechanism working — keep
watching the README "Tracking patterns" section during UI/.

## Git state at session end

Branch: `main`. No code commits during the review — findings are
documentation; implementation happens in later passes. Working-tree
modifications remain docs-only (`Docs/` + the earlier `CLAUDE.md` edit
from the 2026-05-28 session, still uncommitted). The known blocker
**F-47** (invisible gem pack) is the one item that is a live gameplay
bug rather than a code-quality finding.
