# Session 2026-09-05 — catalog completion, economy pass, shop polish, game modes, help

## Landed (committed + pushed 2026-09-05)
- **Skin catalog 100% complete**: art re-sourced from `RawArt/iNGREDEINTS REVISED` (proper
  names), Blue Cheese + Jalapeño added, the real Integral bottom replaced the Brioche stand-in.
  Shop rows ordered buns → level-1 → by appearance. No missing skin art remains.
- **Economy study** (`Docs/economy-2026-09-05.md`) + retunes: consumables 100/270/700★,
  gem-cheap skins 30→60◆ (chef European 50→80◆), FREE rewarded-gems rung capped 3/day
  (persisted in SaveDataManager). Remove Ads banner recomposed from shop widgets (store's
  localized price on the pill, red ONE TIME BUY corner tag) — the baked mock art is retired.
- **Currency symbols saga**: Panton trial lacks € entirely and slivers $ → LiberationSans SDF
  wired as global TMP fallback + a hand-authored sticker material. Final call: money labels are
  **digits-only** (`MoneyLabel` strips store-localized strings too); the sticker material still
  renders the banner "+" and the help pager "/" via rich-text font tags.
- **Shop polish** (two screenshot rounds): one lateral span for every section, thick pills
  overlapping the box bottoms, names on the box top edges, centered pill faces, previews seated
  on bigger plates, bun pairs sized by width, larger pack/power-up icons and qty text, merch
  badges dropped, subtitles dropped, WATCH AD on the standard green pill.
- **Game modes**: Classic/Relax — menu Settings toggle (next-run apply). Relax = same speed
  curve, thresholds ×3 (`RELAX_LENGTH_SCALE`, kill screen included), all in-run star income
  ×0.5 at the single `AwardStars` faucet, no high-score writes. Red CLASSIC/RELAX tab
  (Level/Score tab recipe) on the Special Order card's bottom edge.
- **Settings**: in-game Restart removed — full-width Quit to Menu (interstitials now trigger
  only on the game-over Retry).
- **How to Play**: "?" top-bar button (menu + in-game, one shared placement — the menu's
  bigger-gear override was dropped for consistency) opens a paginated modal: 6 pages of lime
  headers + dash bullets flowing through a VerticalLayoutGroup + ContentSizeFitter (auto-height,
  no overlap), "1/6" pager with rotated yellow arrows.
- **Debug**: V forces game over (editor-only), for screen inspection.
- **Screen quality pass COMPLETE**: menu, in-game HUD, shop, settings, credits, game over —
  all approved by Oscar.

## Decisions
- Ad pacing is now single-door (game-over Retry only). If ads feel too sparse in playtests,
  use a time cooldown at break points — don't gate more buttons.
- Body text is never `FontStyles.Bold` while the trial font lives (synthetic bold on ExtraBold
  smears strokes).
- Currency symbols stay off money labels until the real font; the purchase sheet is the source
  of truth.
- TopBar help/gear buttons share one placement on every screen — no per-screen overrides.

## Pending / next session
- **NEXT: Google Play app setup + payments** — create the Play Console app, the 5 IAP products
  (`gems_100/550/1200/2600`, `remove_ads` — ids must match `MonetizationConfig`), internal
  track + license testers, first .aab (keystore in `Keys/`, gitignored). Copy and answers ready
  in `Docs/play-store-listing.md`; broader items in `Docs/pre-launch-checklist.md`.
- Playtest-pending: the two-type mustard (see `session-2026-09-05-consumables.md`), a full
  Relax run end-to-end.
- Then: on-device LevelPlay test (`LEVELPLAY_TEST_SUITE = true`), iOS ad credentials, IAP
  receipt-validation tangle, font license decision, `SETTINGS_LEVEL_CAP` flip at release.
