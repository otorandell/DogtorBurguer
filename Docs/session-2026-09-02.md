# Session Context — 2026-09-02 (Unity IAP 5 migration)

Branch `iap`, continuing from 2026-09-01. Oscar flagged that **Unity IAP 4 left support on
June 8, 2026** — IAP 5 is the way forward.

- `com.unity.purchasing` **4.12.2 → 5.4.2** in the manifest (latest per the Unity registry;
  needs 2022.3+, we're on 6000.3). The 5.4.2 source was pulled from the registry tarball into
  the scratchpad and `UnityIapProvider` was rewritten against the REAL 5.x API:
  `UnityIAPServices.StoreController()` → subscribe events → `Connect()` (retry on exception) →
  `OnStoreConnected` → `FetchProducts(List<ProductDefinition>)` → `OnProductsFetched` →
  `FetchPurchases()`. Purchases: `PurchaseProduct(product)`; **grants in `OnPurchasePending`**
  (fires for fresh, replayed AND restored orders) then `ConfirmPurchase(order)` — an
  unconfirmed order replays next launch (crash safety). `OnPurchaseFailed(FailedOrder)` →
  Cancelled/Failed. `RestoreTransactions` is cross-platform in IAP 5 (no Apple extension).
  Prices: `GetProductById(id).metadata.localizedPriceString`.
- Interface/manager/mock/shop unchanged — the migration stayed inside the provider. ✔
- Earlier the same day (still IAP 4): the `#if` guard was fixed to
  `ENABLE_CLOUD_SERVICES_PURCHASING` (Unity's actual define; `UNITY_PURCHASING` never reaches
  game assemblies) and Unity's import artifacts committed (`BillingMode.json` = GooglePlay).
- ⚠️ Unverified until the editor resolves 5.4.2: (1) the define survives the 4→5 package swap
  (if not: add it to ProjectSettings scripting defines), (2) the provider compiles in-editor
  (written from package source, so slips should be minor). Then the editor fake-store test:
  gem pack, Remove Ads, Restore.
