# Session Context — 2026-09-01 (Settings panel art integration)

Branch `settings-screen` (off `main`, which was fast-forwarded to the `screens` work first —
menu + LevelPlay + Game Over). Workflow from here: one branch per screen, merge to `main` when
the screen is done. Remaining screens after this: **Credits**, **Shop** (mocks to come).

## Settings panel (`UI/SettingsPanel.cs`, rewritten)
Mock: `Look Reference/settings.png` (536x948 ≈ the 540x960 reference, so mock px ≈ reference
px). Same recipe as the Game Over screen — see CLAUDE.md → Pending Features → "Settings panel".
- New imports (`scratchpad/gen_settings_art.ps1`): `ui_settings_panel` (Settings_Background,
  full-phone canvas, 4096 max), `ui_btn_blue_wide` (Settings_Button 1715x494 — the wide row
  blank), `ui_btn_close_x` (Exit button 469x467 — round red X).
- Layout: title (0, 176) on the tab; X at (216, 215) h84; rows 380 wide at y 47 / −65 / −177
  (pitch 112); in-game the third row is the Restart | Quit pair (183 wide, ±98). All in
  `UIStyles.SETTINGS_*`.
- **Level selector is NOT part of the final panel** (Oscar, 2026-09-01). Kept as a testing tool
  behind an inspector bool: `MainMenuUI` → **Show Level Stepper** (off by default; a first cut
  used `#if UNITY_EDITOR`, which still showed it in the editor — Oscar asked for a toggle).
  Flat widgets at y −330 under the art, menu panel only. `SETTINGS_LEVEL_CAP` flip before
  release still applies.
- **"Language: ENG" row skipped** — no localization system; flagged in CLAUDE.md.
- Shared bits: `UIStyles.GAMEOVER_OVERLAY` → `MODAL_OVERLAY` (game over + settings);
  `AnimConfig.GAMEOVER_*` → `PANEL_*` pop-in (both panels). Removed old flat-panel knobs
  (`INNER_PANEL_BG`, `PANEL_TITLE_SIZE`, `SETTINGS_BUTTON_TEXT_SIZE`, `SETTINGS_BUTTON_SIZE`,
  `CLOSE_BUTTON_SIZE`, `SETTINGS_PANEL_SIZE*`, `SETTINGS_*_POS` for the old rows);
  `BTN_SETTINGS_TOGGLE` → `BTN_DEV_STEPPER`.
- Verified with `dotnet build` on Unity's csproj: **0 errors**.

⚠️ **Not yet playtested** (neither is the Game Over screen from 2026-08-31). On next editor focus:
3 new sprites import + recompile. Check both openers (menu gear / in-game gear → pause →
resume on X), Sound/Controls toggles relabel, in-game Restart (interstitial cadence) and Quit,
the dev stepper under the panel, and the pop-in over a paused game (unscaled). Tune
`SETTINGS_*` live.

## Credits panel (branch `credits-screen`, after `settings-screen` merged to main)
Mock: `Look Reference/Credits.png`. Built on a new shared **`UI/ModalPanel.cs`** (extracted from
SettingsPanel: overlay + `ui_modal_panel` art [renamed from `ui_settings_panel`, guid kept] +
title + X + pop-in; `UIStyles.MODAL_*`). `CreditsPanel` = three `CreditsEntry` lines (role
heading in an accent color over a pastel checkered band with the name); the band is
`SpriteFactory.Checker` tinted per entry (no band art in the kit). Replaces the old dark
text-overlay credits in `MainMenuUI`. Knobs: `UIStyles.CREDITS_*` (eyeball from the mock).
- **Music credit — resolved**: Oscar found the five OpenGameArt sources; mapped to our files by
  size + tags in `Docs/music-attribution.md`. Five artists (SketchyLogic, BossLevelVGM, Martin
  Nilsson, Alex McCulloch, Spring Spring), three CC-BY 3.0 → credit required. The MUSIC BY band
  now carries all five over three lines: the band grows per extra line and the name auto-fits
  (Oscar: "reduce the font for them"). Store-listing credit line noted in the doc for launch.
- **Bug fixed**: `ModalPanel.Build` deactivated the root before screens added texts → TMP Awake
  never ran → `tmp.font` null in `StyleFillAndBorder` (NRE on opening Credits). Build stays active.
- Compile-checked (`dotnet build` on a patched csproj copy): 0 errors. **Not playtested.**

## Shop restyle (branch `shop-screen`, after `credits-screen` merged to main)
Mocks: `Look Reference/Shop_example_1..3.png` (the page, three scroll positions) +
`Shop buy confirm.png`. See CLAUDE.md → Core Systems → Shop for the result. Key points:
- **No shop art in the kit** (checked all three drops + the `UI/*Kit*.png` sheets — the button
  sheet has the words baked in). Derived stand-ins with Pillow (`scratchpad/build_shop_art.py`):
  the tall page is the modal panel sheet with its own flat body tiled taller (seams invisible —
  the body is near-uniform cream with very faint dots); green/red wide pills are hue-shifts of
  the blue blank. Borrowed icons for packs. Swap list in CLAUDE.md.
- Layout to the mock: page + tab + X, TopBar pills inside the page, 3-column grids
  (`GridLayoutGroup`) for power-ups / stars / gems, h-scroll rows for skins, the **Pro Cook
  Pack** bundle (new `MonetizationConfig.PRO_COOK_PACK` = 3 of each for 1000★ +
  `ShopService.TryBuyProCookPack`), the THANK YOU banner state after Remove Ads, the confirm
  dialog as a cream card with icon lines and BUY/CANCEL pills.
- New `Shop/ShopCell.cs` (cell parts); `ShopWidgets` gained `CreateGrid`, `CreateBox` (9-sliced
  `ui_consumable_box` — border added to its meta, the consumable slots draw it Simple so they're
  unaffected), `CreatePill`/`SetPillLabel`, `CreateIconLine`, `CellHeight`, `StyleAccent`,
  `MoneyLabel` ("$" stripped — trial-font sliver glyph).
- Removed dead knobs: `SHOP_BG/CARD_*`, `BTN_SHOP_*`, `OVERLAY_DARK`, `PANEL_BG`, `BTN_CLOSE`,
  `BTN_RESTART`, `PANEL_BUTTON_TEXT_SIZE`.
- Compile-checked (patched csproj copy): 0 errors / 0 warnings. **Not playtested** — the grid /
  pill / icon-line layout is the riskiest part (layout groups + TMP preferred sizes); expect a
  tuning pass on `SHOP_*`.

## Next
1. Playtest the Shop (both openers), tune `SHOP_*`; ask the artist for the shop pieces.
3. Wrap-up list unchanged (font license swap, economy, `SETTINGS_LEVEL_CAP`, IAP, consent,
   on-device LevelPlay test).
