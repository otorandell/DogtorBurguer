namespace DogtorBurguer
{
    /// <summary>The shipped languages. Order is the Settings-row cycle order; the int values
    /// are persisted by SaveDataManager, so only append — never reorder.</summary>
    public enum Language
    {
        English = 0,
        Spanish = 1,
        Portuguese = 2,
        German = 3,
        French = 4,
        Italian = 5,
        Turkish = 6,
    }
}
